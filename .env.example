# ===========================================
# Agent 系统环境变量
# 所有应用配置已迁移到 config/config.json（复制 config/config.example.json 并修改）
# 此文件仅保留非配置类的环境变量
# ===========================================

# 学习模式（每轮任务结束后自动反思），默认关闭
LEARNING_PER_ROUND=false
